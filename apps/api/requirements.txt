fastapi>=0.115.0
uvicorn[standard]>=0.32.0
# `[email]` pulls in `email-validator`, required by pydantic's EmailStr
# type used in app/domain/models.py (User.email). Without this extra the
# app raises ImportError at startup before uvicorn binds, and Railway's
# healthcheck times out.
pydantic[email]>=2.9.2
pydantic-settings>=2.6.1
firebase-admin>=6.5.0
python-multipart>=0.0.20
httpx>=0.27.2
python-slugify>=8.0.4

# Face detection + recognition (InsightFace stack)
# InsightFace's `app.FaceAnalysis` orchestrates detection (SCRFD) + recognition
# (ArcFace) via ONNX models. We pin to CPU ORT for portability — switching to
# GPU is a Dockerfile change (onnxruntime-gpu + CUDA base image).
insightface>=0.7.3
onnxruntime>=1.17.0
opencv-python-headless>=4.8.0
numpy>=1.24,<2.0
Pillow>=10.0.0
